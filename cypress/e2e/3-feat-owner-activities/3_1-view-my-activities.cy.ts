/**
 * Given an activity owner
 *  When visiting the activities mines page
 *   Then should see its own activities on any state
 *   Then should see activities name, price, and date
 *   Then should have select and edit buttons
 *
 *  When selecting an activity
 *   Then should see its participants below
 *
 *  When editing an activity
 *   Then should navigate to the edit page
 */
